// Package agent holds the serializable agent definition used by
// deployment documents: how a document describes an agent's card,
// engine selection, tool allow-list, resource bindings, and lifecycle
// hooks. The runtime execution contract (Engine, Host, Run) will live
// in this package as well.
package agent
