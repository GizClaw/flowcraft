apiVersion: vessel.flowcraft.io/v1alpha1
kind: LLMProfile
metadata:
  name: shared-openai
spec:
  provider: openai
  config:
    defaultModel: gpt-5
  auth:
    apiKey:
      valueFrom:
        env: OPENAI_API_KEY
