# Eval-local artifacts; not tracked.

# Per-run SimpleQA report JSON.
simpleqa/results/

# CLI build product: `go build ./cmd/eval` from eval/ drops the binary
# at eval/eval. The leading slash anchors this to the module root.
/eval
