domain: task
events:
  task.submitted:
    version: 1
    category: business
    partition: card
    payload_ref: P
    producers: [test]
    consumers: [test]
    transports: [ws]
    audit_required: false
    doc: "changed field type at same version (forbidden)"
payloads:
  P:
    card_id: { type: int64, required: true }
    title:   { type: string, required: true }
