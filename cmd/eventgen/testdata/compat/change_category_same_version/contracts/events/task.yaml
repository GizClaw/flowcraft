domain: task
events:
  task.submitted:
    version: 1
    category: audit
    partition: card
    payload_ref: P
    producers: [test]
    consumers: [test]
    transports: [ws]
    audit_required: true
    audit_summary: "task {{payload.card_id}} submitted"
    doc: "category changed at same version (forbidden)"
payloads:
  P:
    card_id: { type: string, required: true }
    title:   { type: string, required: true }
