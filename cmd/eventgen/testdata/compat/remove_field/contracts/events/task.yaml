domain: task
events:
  task.submitted:
    version: 1
    category: business
    partition: card
    payload_ref: P
    producers: [test]
    consumers: [test]
    transports: [ws]
    audit_required: false
    doc: "removed a baseline field at same version (forbidden)"
payloads:
  P:
    card_id: { type: string, required: true }
