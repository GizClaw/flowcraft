domain: task
events:
  task.Submitted:
    version: 1
    category: business
    partition: card
    payload_ref: P
    producers: [test]
    consumers: [test]
    transports: [ws]
    audit_required: false
    doc: "uppercase in event name"
payloads:
  P:
    card_id: { type: string, required: true }
