domain: task
events:
  task.submitting:
    version: 1
    category: business
    partition: card
    payload_ref: P
    producers: [test]
    consumers: [test]
    transports: [ws]
    audit_required: false
    doc: "progressive -ing suffix"
payloads:
  P:
    card_id: { type: string, required: true }
