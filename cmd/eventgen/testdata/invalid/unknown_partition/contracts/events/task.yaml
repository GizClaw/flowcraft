domain: task
events:
  task.submitted:
    version: 1
    category: business
    partition: nonexistent
    payload_ref: P
    producers: [test]
    consumers: [test]
    transports: [ws]
    audit_required: false
    doc: "partition not registered in manifest"
payloads:
  P:
    card_id: { type: string, required: true }
