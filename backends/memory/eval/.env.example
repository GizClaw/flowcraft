# Copy to the repository root as .env (git-ignored) and fill in.
DEEPSEEK_API_KEY=
# ByteDance Ark multimodal embeddings for the vector lane
# (doubao-embedding-vision).
ARK_API_KEY=
# Optional: only needed when a run selects -answer-provider openai (gpt-4o-mini).
OPENAI_API_KEY=
